cubes:
  - name: b2b_customer_addresses
    sql_table: demo.b2b_customer_addresses

    joins: []

    dimensions:
      - name: address_id
        sql: address_id
        type: number
        primary_key: true

      - name: customer_id
        sql: customer_id
        type: number

      - name: addr1
        sql: addr1
        type: string

      - name: addr2
        sql: addr2
        type: string

      - name: address_label
        sql: address_label
        type: string

      - name: addressee
        sql: addressee
        type: string

      - name: addrphone
        sql: addrphone
        type: string

      - name: city
        sql: city
        type: string

      - name: country
        sql: country
        type: string

      - name: defaultbilling
        sql: defaultbilling
        type: boolean

      - name: defaultshipping
        sql: defaultshipping
        type: boolean

      - name: full_address
        sql: full_address
        type: string

      - name: isresidential
        sql: isresidential
        type: boolean

      - name: links
        sql: links
        type: string

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

