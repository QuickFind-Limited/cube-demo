cubes:
  - name: locations
    sql_table: demo.locations

    joins: []

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true

      - name: mainaddress
        sql: mainaddress
        type: number

      - name: subsidiary
        sql: subsidiary
        type: number

      - name: isinactive
        sql: isinactive
        type: boolean

      - name: links
        sql: links
        type: string

      - name: name
        sql: name
        type: string

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

